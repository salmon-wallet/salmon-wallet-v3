# Tasks

## Implementation

- [x] Add Set-based deduplication filter in `useTransactions.ts` loadMore append path (line 186) to filter out transactions with duplicate `id` before concatenating with existing state
