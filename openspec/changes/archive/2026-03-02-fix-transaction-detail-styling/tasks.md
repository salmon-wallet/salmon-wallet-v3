# Tasks

## Web/Extension (packages/ui)

- [x] Replace `CardContainer` with `BlurContainer` in `TransactionDetailModal.tsx` for all section cards
- [x] Change `HashValue` font from `fontFamily.mono` to `fontFamily.sans` in `TransactionDetailModal.tsx`
- [x] Change all `colors.accent.primary` to `colors.palette.amber` in web `ExplorerLinkButton.tsx`

## Mobile (apps/mobile)

- [x] Change all `colors.accent.primary` to `colors.palette.amber` in mobile `ExplorerLinkButton.tsx`
