/**
 * GlassTabBar Component
 *
 * Glass-effect bottom tab bar with Liquid Glass support on iOS 26+.
 * Falls back to BlurView on other platforms.
 *
 * @module components/GlassTabBar
 */

export { GlassTabBar, default } from './GlassTabBar';
export type { GlassTabBarProps, TabConfig } from './types';
