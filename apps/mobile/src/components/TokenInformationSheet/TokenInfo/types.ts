import type { ViewStyle } from 'react-native';
import type { TokenInfoPropsBase } from '@salmon/shared';

/**
 * Props for the TokenInfo component (React Native)
 */
export interface TokenInfoProps extends TokenInfoPropsBase<ViewStyle> {}
