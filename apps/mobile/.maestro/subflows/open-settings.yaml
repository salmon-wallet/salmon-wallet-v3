appId: io.salmonwallet.app
---
# Assumes Home (account switcher chip visible). Opens the Settings sheet.
- assertVisible:
    id: "wallet-header-account-switcher"
- tapOn:
    id: "wallet-header-settings-button"
- extendedWaitUntil:
    visible: "Settings"
    timeout: 5000
