# Salmon Web Playwright test secrets — example file.
# Copy to .env.test (gitignored) and fill before running any web e2e script.
#
# Add the variables your scripts will need; this stub mirrors the extension
# suite's pattern. Update the contract in scripts/lib.mjs/loadSecrets when
# you start the web suite.

# Password for any wallet recovery the web flow requires (currently unused).
SALMON_TEST_PASSWORD=ReplaceMe!
