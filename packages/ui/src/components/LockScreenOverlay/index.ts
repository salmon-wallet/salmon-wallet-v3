/**
 * LockScreenOverlay Component
 *
 * Animated lock screen overlay with slide animation,
 * password unlock, and optional biometric support.
 *
 * @module components/LockScreenOverlay
 */

export { LockScreenOverlay, default } from './LockScreenOverlay';
export type {
  LockScreenOverlayProps,
  BiometricConfig,
  BiometricAuthState,
} from './types';
